# Vercel Environment Variables
# Set these in Vercel Project Settings > Environment Variables
#
# VITE_ prefixed vars are baked in at build time by Vite.
# Both customer and admin apps read VITE_API_BASE_URL.

VITE_API_BASE_URL=https://auan-api.onrender.com/api/v1
