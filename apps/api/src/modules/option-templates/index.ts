/**
 * Option Templates module — public API for registration in server.ts.
 *
 * Exports:
 *   optionTemplateRoutes — route registration (controllers + handlers)
 */

export { optionTemplateRoutes } from "./option-template.routes.js"
