/**
 * Addresses module — public API for registration in server.ts.
 *
 * Exports:
 *   addressRoutes — route registration (controllers + handlers)
 */

export { addressRoutes } from "./addresses.routes.js"
