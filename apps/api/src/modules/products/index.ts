/**
 * Product module — public API for registration in server.ts.
 *
 * Exports:
 *   productRoutes — route registration (controllers + handlers)
 */

export { productRoutes } from "./products.routes.js"
