/**
 * Category module — public API for registration in server.ts.
 *
 * Exports:
 *   categoryRoutes — route registration (controllers + handlers)
 */

export { categoryRoutes } from "./categories.routes.js"
