/**
 * Payments module — public API for registration in server.ts.
 *
 * Exports:
 *   paymentRoutes — route registration (controllers + handlers)
 */

export { paymentRoutes } from "./payments.routes.js"
