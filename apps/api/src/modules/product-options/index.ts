/**
 * Product Options module — public API for registration in server.ts.
 *
 * Exports:
 *   productOptionRoutes — route registration (controllers + handlers)
 */

export { productOptionRoutes } from "./product-options.routes.js"
