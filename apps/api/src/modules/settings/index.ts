/**
 * Settings module — public API for registration in server.ts.
 *
 * Exports:
 *   settingsRoutes — route registration (controllers + handlers)
 */

export { settingsRoutes } from "./settings.routes.js"
